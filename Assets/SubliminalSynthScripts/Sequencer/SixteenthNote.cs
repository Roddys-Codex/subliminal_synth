using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SixteenthNote : MonoBehaviour
{

    public int note = 60;
    public float start;
    public float end;
    public float velocity = 1.0f;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
